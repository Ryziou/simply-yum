<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/main.css">
    <title>Edit Recipe | Simply Yum</title>
</head>
<body>
    <%- include('../partials/nav') %>
    <form action="/recipes/<%= recipe._id %>?_method=PUT" method="POST" enctype="multipart/form-data">

        <div class="input-control">
            <label for="title">Title:</label>
            <input type="text" name="title" id="title" placeholder="Name of the recipe" value="<%= recipe.title %>">
        </div>

        <div class="input-control">
            <label for="description">Description:</label>
            <textarea name="description" id="description" placeholder="Details about the recipe" rows="5" cols="50"><%= recipe.description %></textarea>
        </div>

        <div class="input-control">
            <label for="category">Category:</label>
            <select name="category" id="category">
                <option value="breakfast" <%= recipe.category === 'breakfast' ? 'selected' : '' %>>Breakfast</option>
                <option value="main" <%= recipe.category === 'main' ? 'selected' : '' %>>Main</option>
                <option value="dessert" <%= recipe.category === 'dessert' ? 'selected' : '' %>>Dessert</option>
            </select>
        </div>

        <div class="input-control">
            <label for="imageUrl">Food Image:</label>
            <img src="<%= recipe.imageUrl %>" alt="Preview" width="300">
            <input type="file" name="imageUrl" id="imageUrl" accept="image/*">
        </div>

        <div class="input-control">
            <label>Ingredients:</label>
            <input type="text" name="ingredients" placeholder="e.g. 2 cups flour" value="<%= recipe.ingredients[0] %>">
            <input type="text" name="ingredients" placeholder="e.g. 1 tsp salt" value="<%= recipe.ingredients[1] %>">
            <input type="text" name="ingredients" placeholder="e.g. 3 eggs"  value="<%= recipe.ingredients[2] %>">
            <button type="button" id="add-ingredients">+</button>
        </div>
        
        <div class="input-control">
            <label>Instructions:</label>
            <input type="text" name="instructions" placeholder="Step 1" value="<%= recipe.instructions[0] %>">
            <input type="text" name="instructions" placeholder="Step 2" value="<%= recipe.instructions[1] %>">
            <input type="text" name="instructions" placeholder="Step 3"  value="<%= recipe.instructions[2] %>">
            <button type="button" id="add-instructions">+</button>
          </div>

        <div class="input-control">
            <label for="cookTime">Cook Time (minutes):</label>
            <input type="number" name="cookTime" id="cookTime" value="<%= recipe.cookTime %>">
        </div>

        <div class="input-control">
            <label for="servings">Servings:</label>
            <input type="number" name="servings" id="servings" value="<%= recipe.servings %>">
        </div>


        <button type="submit">Submit</button>

    </form>
</body>
</html>