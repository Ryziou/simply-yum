<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/main.css">
    <link rel="stylesheet" href="/stylesheets/pages/auth.css">
    <title>Sign In | Simply Yum</title>
</head>

<body>
    <%- include('../partials/nav') %>
        <div class="container">
            <div class="auth-header">
                <h1>Sign In</h1>
            </div>
            <form action="/auth/sign-in" class="auth-form" method="POST">
                <div class="input-control">
                    <input type="text" name="identity" id="identity" placeholder="Email or Username" required>
                </div>

                <div class="input-control">
                    <input type="password" name="password" id="password" placeholder="Password" required>
                </div>

                <% if (errorMessage) { %>
                    <p class="error-message">
                        <%= errorMessage %>
                    </p>
                <% } %>
                <div class="login-btn">
                    <button type="submit" class="btn-signin">Sign In</button>
                </div>
            </form>
        </div>
</body>

</html>