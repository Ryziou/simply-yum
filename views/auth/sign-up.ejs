<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/main.css">
    <title>Create Account | Simply Yum</title>
</head>
<body>
    <%- include('../partials/nav') %>
    <h1>Create Account</h1>
    <form action="/auth/sign-up" method="POST" enctype="multipart/form-data">
        <div class="input-control">
            <label for="email">Email:</label>
            <input type="email" name="email" id="email" required>
        </div>

        <div class="input-control">
            <label for="username">Username:</label>
            <input type="text" name="username" id="username" required>
        </div>

        <div class="input-control">
            <label for="password">Password:</label>
            <input type="password" name="password" id="password" required>
        </div>

        <div class="input-control">
            <label for="passwordChecker">Confirm Password:</label>
            <input type="password" name="passwordChecker" id="passwordChecker" required>
        </div>

        <div class="input-control">
            <label for="avatar">Profile Image</label>
            <input type="file" name="avatar" id="avatar" accept="image/*">
        </div>

        <% if (errorMessage) { %>
         <p class="error-message"><%= errorMessage %></p>
        <% } %>

        <button type="submit">Sign Up</button>
    </form>
</body>
</html>